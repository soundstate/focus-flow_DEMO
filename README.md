# Focus Flow - Interactive Productivity Timer

> **RETIRED 2026-06-22: folded into cadence.**

focus-flow is no longer developed as a standalone project. It has been merged into **cadence** (the SSLLC AI focus co-pilot), which is now the single home for this work.

- **Active repo:** [`ssllc-cadence`](https://github.com/soundstate/ssllc-cadence) (local `O:\ssllc-cadence`).
- **What carried over:** the focus/Pomodoro timer executor, the API-managed Timer (presets 50/10 and 90/5/55), gamification, analytics, the AI coach, and music are all ported into cadence backend modules and shipped to `main` (merge commit `0d9b0934`, 2026-06-22).
- **What has NOT carried over yet:** the frontend product screens in `ui/focus-flow-app` (Timer, Dashboard, Analytics, Templates, Settings). cadence has a fresh frontend foundation (auth, app shell, typed API client); these screens remain here as a UX reference for rebuilding against the new Timer API.
- **Canon:** the cadence element note in Notion. Background: the cadence + focus-flow merge analysis (2026-06-21).

This repository is archived (read-only) for historical reference. Do not start new work here. All development happens in cadence.
